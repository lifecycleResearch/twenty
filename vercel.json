{
  "installCommand": "npm install -g corepack && corepack enable && yarn workspaces focus twenty-front --production",
  "buildCommand": "yarn workspace twenty-front build",
  "outputDirectory": "packages/twenty-front/dist"
}
